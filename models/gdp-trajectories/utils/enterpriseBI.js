/**
 * initializeLiveBIStream()
 *
 * ENTERPRISE / COMMERCIAL LICENCE FEATURE
 * ────────────────────────────────────────
 * This function is a reserved stub for the
 * Commercial Enterprise licence tier.
 *
 * When implemented under commercial licence,
 * this module will provide:
 *
 *   - Direct live API streaming from the
 *     simulation engine into Power BI Service
 *     cloud environments via Power Query /
 *     M code data source connectors
 *
 *   - Secure authenticated endpoints for
 *     corporate Power BI datasets to query
 *     live simulation state without local
 *     file downloads
 *
 *   - Team workspace synchronisation so
 *     multiple analysts can share and fork
 *     scenario configurations in real time
 *
 *   - SSO integration for enterprise identity
 *     providers (Azure AD, Okta, etc.)
 *
 * TO ENQUIRE ABOUT COMMERCIAL LICENSING:
 *   license@dataisc.dev
 *   See /licence in the simulator UI
 *
 * DO NOT IMPLEMENT THIS FUNCTION in the
 * open source version. It must remain a stub.
 */
export function initializeLiveBIStream(config) {
  console.warn(
    '[ENTERPRISE FEATURE] initializeLiveBIStream ' +
    'requires a commercial licence. ' +
    'Contact license@dataisc.dev'
  );
  return null;
}
